import type { NextConfig } from "next";
module.exports = {
  images: {
    domains: ["https://res-console.cloudinary.com/*"], // Add the Cloudinary domain
  },
};
const nextConfig: NextConfig = {};

export default nextConfig;
